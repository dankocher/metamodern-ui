<!--- MetPromptInfo.stories.mdx -->

import { Meta, ArgsTable, Preview, Story } from "@storybook/addon-docs/blocks";
import { MetPromptInfo } from "./index";

<Meta title="MDX/MetPromptInfo" component={MetPromptInfo} />

## MetPromptInfo

### Import

```jsx
import { MetPromptInfo } from "@metamodern.dev/metamodern-ui/";
```

### Usage

```jsx
<MetPromptInfo
    style={style}
    className={className}
    fontClass={fontClass}
    value={value}
    isOpen={isOpen}
    
    onClick={onClick}
    icon={icon}
    bgColor={bgColor}
    hoverColor={hoverColor}
    defaultIconColor={defaultIconColor}
    size={size}
    styleIconBtn={styleIconBtn}
    classNameIconBtn={classNameIconBtn}
/>
```

### Props

<ArgsTable of={MetPromptInfo} />
