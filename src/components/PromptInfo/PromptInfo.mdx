<!--- MetPromptInfo.stories.mdx -->

import { Meta, ArgsTable, Preview, Story } from "@storybook/addon-docs/blocks";
import { MetPromptInfo } from "./index";

<Meta title="MDX/MetPromptInfo" component={MetPromptInfo} />

## MetPromptInfo

### Import

```jsx
import { MetPromptInfo } from "./MetPromptInfo";
```

### Usage

```jsx
<MetPromptInfo
    onClick={onClick}
    icon={icon}
    style={style}
    className={className}
    fontClass={fontClass}
    value={value}
    isOpen={isOpen}
    bgColor={bgColor}
    hoverColor={hoverColor}
    defaultIconColor={defaultIconColor}
    size={size}
/>
```

### Props

<ArgsTable of={MetPromptInfo} />
