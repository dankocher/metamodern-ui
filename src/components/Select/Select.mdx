<!--- MetSelect.stories.mdx -->

import { Meta, ArgsTable, Preview, Story } from "@storybook/addon-docs/blocks";
import { MetSelect } from "./index.ts";

<Meta title="MDX/MetSelect" component={MetSelect} />

## MetSelect

### Import

```jsx
import { MetSelect, SelectState } from "@metamodern.dev/metamodern-ui/";
```

### Usage

```jsx
<MetSelect
    style={style}
    className={className}
    selectorFontClass={selectorFontClass}
    onChange={onChange}
    isDisabled={isDisabled}
    label={label}
    icon={icon}
    state={SelectState.DEFAULT}
    placeholder={placeholder}
    multiSelect={multiSelect}
    borderColor={borderColor}
    selectedColor={selectedColor}
    hoverColor={hoverColor}
    placeholderColor={placeholderColor}
    errorColor={errorColor}
    successColor={successColor}
/>
```

### Props

<ArgsTable of={MetSelect} />
