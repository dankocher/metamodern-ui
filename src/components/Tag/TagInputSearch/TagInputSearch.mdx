<!--- MetTagInputSearch.stories.mdx -->

import { Meta, ArgsTable, Preview, Story } from "@storybook/addon-docs/blocks";
import { MetTagInputSearch } from "./index.ts";

<Meta title="MDX/MetTagInputSearch" component={MetTagInputSearch} />

## MetTagInputSearch

### Import

```jsx
import { MetTagInputSearch } from "@metamodern.dev/metamodern-ui/";
```

### Usage

```jsx
<MetTagInputSearch
  style={style}
  value={value}
  searchValue={searchValue}
  onChange={onChange}
  onBlur={onBlur}
  isHasCheckbox={isHasCheckbox}
  onToggle={onToggle}
  isChecked={isChecked}
  isButtonVisible={isButtonVisible}
  checkedIcon={checkedIcon}
  uncheckedIcon={uncheckedIcon}
  defaultFontColor={defaultFontColor}
  defaultBorderColor={defaultBorderColor}
  focusColor={focusColor}
  hoverCheckboxColor={hoverCheckboxColor}
  onClick={onClick}
  icon={icon}
  defaultIconColor={defaultIconColor}
  hoverColor={hoverColor}
  styleIconBtn={styleIconBtn}
  classNameIconBtn={classNameIconBtn}
/>
```

### Props

<ArgsTable of={MetTagInputSearch} />
