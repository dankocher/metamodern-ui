<!--- MetTextArea.stories.mdx -->

import { Meta, ArgsTable, Preview, Story } from "@storybook/addon-docs/blocks";
import { MetTextArea } from "./index.ts";

<Meta title="MDX/MetTextArea" component={MetTextArea} />

## MetTextArea

### Import

```jsx
import { MetTextArea, textFieldState } from "@metamodern.dev/metamodern-ui/";
```

### Usage

```jsx
<MetTextArea
    style={style}
    value={value}
    onChange={onChange}
    onBlur={onBlur}
    isTextSelected={isTextSelected}
    labelChildren={labelChildren}
    placeholder={placeholder}
    isDisabled={isDisabled}
    rowsMins={rowsMins}
    rowsMax={rowsMax}
    state={stateTF.default}
    defaultColor={defaultColor}
    hoverColor={hoverColor}
    focusColor={focusColor}
    errorColor={errorColor}
    successColor={successColor}
/>
```

### Props

<ArgsTable of={MetTextArea} />
