<!--- MetTextField.stories.mdx -->

import { Meta, ArgsTable, Preview, Story } from "@storybook/addon-docs/blocks";
import { MetTextField } from "./index.tsx";

<Meta title="MDX/MetTextField" component={MetTextField} />

## MetTextField

### Import

```jsx
import {
  MetTextField,
  MetTextFieldProps,
  textFieldState as stateTF,
} from "./index";
```

### Usage

```jsx
<MetTextField
  style={style}
  value={value}
  onChange={onChange}
  onBlur={onBlur}
  placeholder={placeholder}
  isDisabled={isDisabled}
  isHaveMessage={isHaveMessage}
  errorIcon={errorIcon}
  successIcon={successIcon}
  errorMessage={errorMessage}
  successMessage={successMessage}
  state={stateTF.default}
  defaultColor={defaultColor}
  hoverColor={hoverColor}
  focusColor={focusColor}
  errorColor={errorColor}
  successColor={successColor}
/>
```

### Props

<ArgsTable of={MetTextField} />
