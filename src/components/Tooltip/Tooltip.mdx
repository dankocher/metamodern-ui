<!--- MetTooltip.stories.mdx -->

import { Meta, ArgsTable, Preview, Story } from "@storybook/addon-docs/blocks";
import { MetTooltip } from "./index";

<Meta title="MDX/MetTooltip" component={MetTooltip} />

## MetTooltip

### Import

```jsx
import { MetTooltip } from "@metamodern.dev/metamodern-ui/";
```

### Usage

```jsx
<MetTooltip
  style={style}
  className={className}
  fontClass={fontClass}
  value={value}
  isOpen={isOpen}
  onClick={onClick}
  onHover={onHover}
  icon={icon}
  bgColor={bgColor}
  bgColorPrompt={bgColorPrompt}
  hoverColor={hoverColor}
  defaultIconColor={defaultIconColor}
  size={size}
  styleIconBtn={styleIconBtn}
  classNameIconBtn={classNameIconBtn}
/>
```

### Props

<ArgsTable of={MetTooltip} />
